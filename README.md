# my_app

A minimal Flutter app scaffold created in the workspace.

## Next steps

1. Install Flutter on Windows: https://flutter.dev/docs/get-started/install/windows
2. Add `C:\src\flutter\bin` (or your SDK path) to your PATH.
3. Restart the terminal.
4. From this folder, run:
   ```powershell
   flutter pub get
   flutter run
   ```
